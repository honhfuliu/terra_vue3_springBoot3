<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ziheng.system.sysrole.mapper.SysRoleMapper">

    <resultMap id="BaseResultMap" type="com.ziheng.system.sysrole.domain.SysRole">
            <id property="roleId" column="role_id" />
            <result property="roleName" column="role_name" />
            <result property="roleKey" column="role_key" />
            <result property="sort" column="sort" />
            <result property="status" column="status" />
            <result property="remark" column="remark" />
            <result property="createTime" column="create_time" />
            <result property="updateTime" column="update_time" />
            <result property="delFlag" column="del_flag" />
    </resultMap>

    <sql id="Base_Column_List">
        role_id,role_name,role_key,sort,status,remark,
        create_time,update_time,del_flag
    </sql>
    <select id="findRolesByUserId" resultType="java.lang.String">
        select role_key from sys_role where role_id in (select role_id from sys_user_role  where user_id = #{userId}) and status = '1' and del_flag = '0';
    </select>

    <delete id="deleteRoleMenu">
        DELETE FROM sys_role_menu WHERE role_id = #{roleId}
    </delete>

    <insert id="insertRoleMenu">
        INSERT INTO sys_role_menu (role_id, menu_id) VALUES
        <foreach collection="menuIds" item="menuId" separator=",">
            (#{roleId}, #{menuId})
        </foreach>
    </insert>

    <select id="selectMenuIdsByRoleId" resultType="java.lang.Long">
        SELECT menu_id FROM sys_role_menu WHERE role_id = #{roleId}
    </select>

    <select id="countUserByRoleId" resultType="java.lang.Long">
        SELECT COUNT(*) FROM sys_user_role WHERE role_id = #{roleId}
    </select>
</mapper>
